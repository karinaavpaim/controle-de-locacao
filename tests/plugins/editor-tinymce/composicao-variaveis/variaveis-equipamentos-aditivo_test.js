import variaveisEquipamentosParaAditivo from '../../../../src/plugins/editor-tinymce/composicao-variaveis/variaveis-equipamentos-aditivo';

describe('Variáveis de equipamentos para aditivo', () => {

  beforeEach(() => {});

  it('Será implementado na proxima tarefa.', () => {
    let variaveis = variaveisEquipamentosParaAditivo.obter();
    expect(variaveis).toEqual(expect.arrayContaining([]));
  });
});